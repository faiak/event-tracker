import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  wrapper: { padding: 13, flex: 1 },
  image: { width: '100%', height: 250, borderRadius: 5 },
  textWrapper: { paddingLeft: 8, paddingTop: 8 },
  name: { fontSize: 32 }
})
