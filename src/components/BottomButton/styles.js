import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  button: {
    margin: 13,
    backgroundColor: 'red',
    alignItems: 'center',
    paddingVertical: 13,
    borderRadius: 13,
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0
  },
  text: { color: 'white', fontSize: 18 }
})
